{
  "name": "artisanal-brew-agent",
  "version": "1.0.0",
  "description": "Daily coffee-brewing simulation agent for Artisanal Brew",
  "type": "module",
  "scripts": {
    "start":     "tsx src/index.ts",
    "dry-run":   "tsx src/index.ts --dry-run",
    "typecheck": "tsc --noEmit",
    "build":     "tsc"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dotenv":     "^16.5.0",
    "tsx":        "^4.19.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
